"""Fleet bring-up: attestation, onboarding, calibration, ops, container.

Subpackages import heavy stacks lazily where the ground rules demand it
(bittensor/aioboto3 behind function calls); ``import fleet`` alone stays cheap.
"""
